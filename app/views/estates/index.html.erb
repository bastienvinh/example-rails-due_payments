<%= render :partial => 'sub_menu' %>
<div class="col-md-9 col-lg-10">
  <div class="table-responsive">
    <table id="table-list-landlord" class="table table-sm table-hover table-striped">
      <thead class="thead-inverse">
        <tr>
          <th>Name</th>
          <th>Acre</th>
          <th>Zip Code</th>
          <th>Address</th>
          <th>Total</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% @estates.each do |estate| %>
          <tr>
            <td><%= estate.name %></td>
            <td><%= estate.acre %></td>
            <td><%= estate.zip_code %></td>
            <td><%= estate.address %></td>
            <td><%= number_to_currency(estate.total_price, unit: '£', delimiter: ' ', separator: ', ', format: "%n %u") %></td>
            <td>
              <a href="#" class="btn btn-link"><span class="ion-edit size-16"></span></a>
              <%= button_to "lala", :class => 'btn btn-link', :style => 'display: inline-block;', :method => 'delete' do %>
                <span class="ion-ios-trash size-16"></span>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>